import { DeleteCustomerPipe } from './delete-customer.pipe';

describe('DeleteCustomerPipe', () => {
  it('create an instance', () => {
    const pipe = new DeleteCustomerPipe();
    expect(pipe).toBeTruthy();
  });
});
